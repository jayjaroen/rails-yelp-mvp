<div class="container">
  <h3 style="text-align:center">⭐️ Leave a Review</h3>

  <%= simple_form_for [ @restaurant, @review ] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "submit a review", class: 'btn btn-primary'%>
  <% end %>
</div>
